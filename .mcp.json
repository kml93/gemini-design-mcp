{
  "mcpServers": {
    "gemini-design": {
      "type": "stdio",
      "command": "sh",
      "args": ["-c", "bun install && bun build/index.js"],
      "env": {
        "API_KEY": "${GEMINI_API_KEY}"
      }
    }
  }
}
